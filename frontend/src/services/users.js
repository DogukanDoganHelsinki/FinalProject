import axios from 'axios'
const url = '/api/users'

// creating a new user - only username and password fields?
const createUser = ( username, password ) => {

}
// login with user
// /login path?
const login = ( username, password ) => {

}

export default {
  createUser,
  login
}